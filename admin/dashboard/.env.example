# Copy to .env.local to override (gitignored).

# Production API — real app users (default for npm run dev via .env.development)
VITE_API_BASE_URL=https://growl-backend.albino-ndreu.workers.dev/api/v1

# Local backend — demo/seed users only (offline development)
# VITE_API_BASE_URL=http://localhost:8787/api/v1
